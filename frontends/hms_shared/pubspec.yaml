name: hms_shared
description: HMS shared package — models, API providers, auth logic
version: 1.0.0

environment:
  sdk: '>=3.12.0 <4.0.0'

dependencies:
  dio: ^5.10.0
  flutter_secure_storage: ^10.3.1
  json_annotation: ^4.9.0

dev_dependencies:
  json_serializable: ^6.8.0
  build_runner: ^2.4.0
