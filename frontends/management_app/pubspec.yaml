name: management_app
description: HMS Management App — Multi-role (Owner, Manager, Receptionist, Service Staff, Housekeeper)
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.12.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  get: ^4.7.3
  dio: ^5.4.0
  intl: ^0.19.0
  hms_shared:
    path: ../hms_shared
  stomp_dart_client: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
